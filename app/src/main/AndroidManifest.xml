<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="piyush_makwana.timemangement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".TimeTable.MainActivity"
            android:label="Your Time Table">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Analysis.AnalysisActivity" />
        <activity android:name=".Goals.GoalsActivity" />
        <activity android:name=".DailyLog.LogActivity" />
        <activity android:name=".TodayView.TodayActivity" />
        <activity android:name=".Main2Activity"></activity>
    </application>

</manifest>